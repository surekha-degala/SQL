CREATE TABLE employee_info(
  id INT PRIMARY KEY, 
  name VARCHAR(100), 
 salary INT 
);
  INSERT INTO employee_info (id, name, salary)
  VALUES (1, suku, 60,000), 
(2, swetha , 80,000), 
(3, surekha, 100,000)
);

SELECT * FROM employee_info;
